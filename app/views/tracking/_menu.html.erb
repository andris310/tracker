<nav>
  <ul>
    <li id='maptrack'><a href='/map'>MapTrack</a></li>
    <li id='add-number'><a>New</a></li>
  </ul>

  <div id="user_nav">
  <% if user_signed_in? %>
    <span class='signed-in-as'><%= current_user.email %> | Not you?</span>
    <span id='sign'>
      <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
    </span>
  <% else %>
    <span id='sign'>
    <%= link_to "Sign up", new_user_registration_path %> <span id='or'>|</span> <%= link_to "Sign in", new_user_session_path %>
  </span>
  <% end %>
  </div>

  <%= render 'track' %>
  <div class="spinner2" style="display: none;">
        <%= image_tag("loader.gif") %>
  </div>

  <span class="error hidden">
    Server is not working. Try again later.
  </span>

</nav>